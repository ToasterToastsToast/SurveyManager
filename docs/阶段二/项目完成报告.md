# 项目完成报告

repo:`https://github.com/ToasterToastsToast/SurveyManager`

## 一、 项目目标
本项目第二阶段的目标是对现有问卷系统进行大规模更新，核心功能围绕“题目管理”展开，实现题目复用、共享、版本控制等高级特性。通过本阶段的开发，旨在：
1. **增强题目管理能力**：实现题目独立存在、复用、共享和版本控制，解决用户重复创建题目的痛点。
2. **提升系统可靠性**：通过版本隔离机制，确保修改题目不会影响已发布问卷，保证数据一致性。
3. **优化用户体验**：建立题库管理系统，方便用户整理和使用常用题目。
4. **扩展统计分析能力**：实现跨问卷的题目统计，为用户提供更全面的数据洞察。
5. **保持系统可扩展性**：在现有架构基础上进行扩展，确保系统能够应对未来的需求变更。

---

## 二、 系统设计
本项目在第一阶段的基础上进行了大规模扩展，保持了原有的技术栈，同时增加了新的功能模块：
* **后端**：继续使用 Python + Flask 框架，扩展了 RESTful API 以支持题目管理、版本控制、题库管理等新功能。
* **数据库**：MongoDB（通过 `pymongo` 驱动交互），按照 v2schema 对数据结构进行了重构。
* **前端**：在原有基础上扩展，增加了题目管理、题库管理、版本历史查看等新界面和交互逻辑。
* **架构亮点**：
  1. **题目与问卷解耦**：题目成为独立的实体，通过 `base_question_id` 关联同一题目的不同版本。
  2. **版本控制机制**：实现了题目版本的创建、追踪和恢复，确保历史问卷不被污染。
  3. **权限管理增强**：支持题目和题库的共享，实现了基于用户的权限控制。
  4. **跨问卷统计**：基于 `base_question_id` 实现了同一题目在多个问卷中的数据汇总。

---

## 三、 MongoDB 设计
系统在第一阶段的基础上，对数据结构进行了重大重构，引入了新的集合和字段：

### 1. 题目集合 (`questions`)
* **核心变更**：
  - 新增 `base_question_id`：同一题目的所有版本共享此ID，用于跨问卷统计。
  - 新增 `version`：版本号，用于追踪题目演进。
  - 新增 `previous_version_id`：上一版本ID，用于追溯修改历史。
  - 新增 `owner_id`：题目创建者，用于权限控制。
  - 新增共享与状态管理字段：`is_public`、`shared_with_users`、`used_in_surveys`。
  - 移除 `is_required`、`order`、`jumps` 字段：这些是“问卷上下文属性”，移至 Survey 表。

### 2. 问卷集合 (`surveys`)
* **核心变更**：
  - 新增 `questions` 数组：维护问卷包含的题目及上下文属性，每个元素包含 `question_id`、`order`、`is_required`、`jumps`。

### 3. 题库集合 (`question_banks`)
* **新增集合**：
  - 包含 `name`、`description`、`owner_id`、`is_public`、`shared_with_users`、`questions` 等字段。
  - `questions` 字段存储 `base_question_id` 列表，永远指向该题目的最新版本。

### 4. 回答集合 (`answers`)
* **核心变更**：
  - 新增 `base_question_id`：题目的基础ID，用于跨问卷统计。

### 5. 用户集合 (`users`)
* **核心变更**：
  - 新增 `question_banks` 字段：用户拥有或加入的题库ID列表。

### 设计优势
1. **版本隔离**：通过独立的题目版本文档，确保修改题目不会影响已发布问卷。
2. **高效统计**：基于 `base_question_id` 的跨问卷统计，避免了复杂的聚合查询。
3. **权限控制**：细粒度的共享机制，支持题目和题库的公开和定向共享。
4. **扩展性**：预留了 `metadata` 字段，为未来功能扩展做准备。

---

## 四、 跳转设计
保持了第一阶段的数据驱动动态跳转设计，同时进行了优化：
1. **数据存储**：跳转规则仍然存储在 `surveys` 集合的 `questions` 数组中，作为每个题目的上下文属性。
2. **前端引擎**：答题时的跳转计算引擎保持不变，在前端（`app.js`）实现。
3. **兼容性**：确保了与第一阶段跳转逻辑的向后兼容，同时支持新版本的题目结构。

---

## 五、 校验设计
在第一阶段的基础上，扩展了校验逻辑：
1. **前端即时校验**：保持了原有的前端校验逻辑，同时适配了新的题目结构。
2. **后端策略校验**：扩展了 `QuestionValidator` 校验策略类，支持新的题目字段和版本控制逻辑。
3. **权限校验**：新增了题目和题库的权限校验，确保用户只能访问和修改有权限的资源。
4. **版本校验**：实现了对已发布问卷中题目修改的拦截，防止历史问卷被污染。

---

## 六、 AI 使用过程
在本项目第二阶段开发中，我继续采用了**多模型对抗**和**严格的 Code Review**策略，与 AI 协作完成了大规模的功能扩展。

**1. AI 做了什么（提供基础建设与提效）：**
* **功能实现**：AI 协助实现了题目分享、历史版本、使用情况、题库管理、跨问卷统计等所有新功能的 API 接口和前端交互逻辑。
* **测试脚本**：AI 参考现有测试结构，创建了覆盖所有新功能的测试脚本 `megatest.py`，极大节省了测试时间。
* **问题排查**：AI 帮助定位和修复了多个前端和后端的问题，如白屏错误、按钮无响应、权限不足等。
* **文档更新**：AI 重写了 README.md 文档，反映 v2 架构与新增功能，同时补充了测试说明与 API 概览。

**2. AI 做错了什么（逻辑盲区与工程缺陷）：**
* **版本管理逻辑错误**：AI 初期实现的版本管理逻辑存在漏洞，导致历史版本丢失和显示混乱。
* **权限控制不完整**：AI 实现的权限控制存在遗漏，导致共享用户无法正常使用题库题目。
* **前端交互问题**：AI 编写的前端代码存在模态框作用域错误、事件绑定冲突等问题，导致部分功能无响应。
* **数据模型不一致**：AI 在实现过程中存在数据模型设计不一致的问题，导致题库版本展示和历史版本混乱。

**3. 我改了什么（主导重构与纠错）：**
* **版本管理重构**：我指导 AI 重构了版本管理逻辑，引入了“写时复制”机制，确保版本链条的完整与准确。
* **权限系统完善**：我要求 AI 系统性扫描权限校验点，统一放宽权限判断逻辑，将“仅所有者”扩展为“所有者或被共享用户”。
* **前端架构优化**：我指导 AI 重构了前端初始化逻辑，引入 URL 驱动状态恢复机制，解决了刷新页面时的模态框错误弹出问题。
* **数据模型对齐**：我要求 AI 进行系统性重构，对集合结构、字段设计、权限、统计逻辑等进行全面修复，确保与 v2schema 一致。

---

## 七、 测试结果

为了验证系统的健壮性，我结合 AI 生成的 `megatest.py` 脚本进行了多维度测试，并在测试中不断闭环修复问题：

1. **自动化 API 单元测试**：运行 `megatest.py`，覆盖了题目创建与复用、题目共享、题目修改隔离、版本管理、多版本并存、题目使用追踪、题库管理、跨问卷统计等核心功能。测试结果显示所有功能均已实现并通过测试。

2. **异常边界测试**：通过脚本故意注入非法数据，验证了后端可以正确拦截并返回 403 状态码，权限控制有效。

3. **端到端集成测试**：运行 `megatest.py` 中的 `TestEndToEnd` 类，验证了完整的题目生命周期流程：创建题目 → 分享 → 复用 → 修改（版本分裂）→ 查询历史 → 恢复 → 查看使用情况 → 跨问卷统计。所有操作均成功完成，数据一致性得到保证。

4. **手工测试**：按照测试说明.md 中的手工测试流程，验证了前端交互及用户体验。测试结果显示：
   - 题目创建与复用功能正常
   - 题目共享功能正常
   - 题目修改隔离机制有效，已发布问卷的题目不可被直接修改
   - 版本管理功能正常，历史记录完整
   - 多版本并存功能正常，不同问卷可以使用不同版本
   - 题目使用追踪功能正常，修改时会提示使用情况
   - 题库管理功能完整，支持创建、添加题目、从题库选题
   - 跨问卷统计功能正常，数据汇总准确

5. **关键风险点测试**：针对版本污染、引用 vs 复制混淆、版本链断裂、统计口径错误、权限问题等关键风险点进行了重点测试，所有风险点均已得到有效控制。

---

## 八、 遇到的工程问题与解决记录

在第二阶段的开发与调试中，我遇到了大量由于系统复杂度上升带来的工程化问题。以下是核心排查与修复过程：

### 问题 1：前端白屏与 404 错误
* **现象与影响**：浏览器访问 http://127.0.0.1:5000/ 时出现白屏，控制台显示 404 错误。
* **技术根因**：Flask 未配置根路径与静态文件路由。
* **解决方案**：在 app.py 中添加根路径返回 index.html、静态文件访问路由及用户登录状态 API，解决白屏 404 问题。

### 问题 2：版本管理逻辑错误
* **现象与影响**：修改题目后历史版本丢失，恢复旧版本时服务器内部错误。
* **技术根因**：版本号计算错误、未更新问卷题目引用、返回语句缺失。
* **解决方案**：修复后端代码中版本号逻辑、补充问卷引用更新逻辑并修正返回语句，引入“写时复制”机制确保版本链条完整。

### 问题 3：权限控制不完整
* **现象与影响**：共享用户无法复用题库题目到问卷中。
* **技术根因**：复用接口权限检查遗漏，未考虑题库层权限。
* **解决方案**：补充“题库层权限”判断，将权限控制从单对象扩展到资源所属容器（题库）层级。

### 问题 4：前端交互问题
* **现象与影响**：填写问卷按钮无反应，刷新页面时错误弹出填写 slug 窗口。
* **技术根因**：前端状态变量与模态框机制缺失，初始化逻辑问题。
* **解决方案**：改用统一的状态控制+模态框方案，重构初始化逻辑，引入 URL 驱动状态恢复机制。

### 问题 5：数据模型不一致
* **现象与影响**：题库“另存为新题”后旧版本丢失，历史版本显示混乱。
* **技术根因**：`base_question_id` 重复导致版本覆盖，“覆盖旧题”时数据仅存入数组而未生成独立文档。
* **解决方案**：重新设计题目的“写时复制”机制，通过在更新前强制创建历史快照，确保版本链条的完整与准确。

### 问题 6：接口语义泛化
* **现象与影响**：更新题目时报缺少 survey_id。
* **技术根因**：后端接口强依赖 survey_id，无法支持题库场景。
* **解决方案**：重构接口以支持题库场景分支，提升接口复用能力。

### 问题 7：版本链数据不完整统计
* **现象与影响**：题目使用情况只显示最后一个问卷。
* **技术根因**：统计逻辑未聚合所有版本的使用记录。
* **解决方案**：改为聚合所有版本的使用记录，确保使用情况统计完整。

### 问题 8：新题错误加入问卷
* **现象与影响**：创建新版本题目时，新题错误地加入了问卷。
* **技术根因**：字段继承问题（survey_id 污染）。
* **解决方案**：修复新题创建逻辑，确保数据隔离。

通过以上问题的排查与修复，系统的稳定性和可靠性得到了显著提升，所有功能均已按照需求变更的要求实现。
